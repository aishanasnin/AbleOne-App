import 'package:flutter/material.dart';

class AppColors {
  // Light Theme Color Brandings
  static const Color primary = Color(0xFF6366F1); // Indigo
  static const Color primaryLight = Color(0xFF818CF8);
  static const Color primaryDark = Color(0xFF4F46E5);
  
  static const Color secondary = Color(0xFF10B981); // Emerald Green
  static const Color secondaryLight = Color(0xFF34D399);
  static const Color secondaryDark = Color(0xFF059669);
  
  static const Color accent = Color(0xFFF59E0B); // Amber
  static const Color accentLight = Color(0xFFFBBF24);
  static const Color accentDark = Color(0xFFD97706);
  
  static const Color background = Color(0xFFF8FAFC); // Cool gray background
  static const Color surface = Colors.white;
  
  static const Color textPrimary = Color(0xFF0F172A); // Slate 900 (High contrast text)
  static const Color textSecondary = Color(0xFF334155); // Slate 700 (High contrast body text)
  static const Color textLight = Color(0xFF64748B); // Slate 500
  static const Color textMuted = Color(0xFF94A3B8); // Slate 400
  
  static const Color border = Color(0xFFE2E8F0); // Slate 200
  static const Color borderFocused = Color(0xFF6366F1); // Primary focus highlights
  
  static const Color error = Color(0xFFDC2626); // Crimson 600 (Accessible error)
  static const Color errorLight = Color(0xFFFEE2E2);
  
  static const Color success = Color(0xFF16A34A); // Emerald 600 (Accessible success)
  static const Color successLight = Color(0xFFDCFCE7);
  
  static const Color warning = Color(0xFFD97706); // Amber 600 (Accessible warning)
  static const Color warningLight = Color(0xFFFEF3C7);
}
